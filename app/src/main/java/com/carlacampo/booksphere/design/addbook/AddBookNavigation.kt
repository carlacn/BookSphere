package com.carlacampo.booksphere.design.addbook
/*
import androidx.navigation.NavController
import androidx.navigation.NavGraphBuilder
import androidx.navigation.NavHostController
import androidx.navigation.compose.composable
import com.carlacampo.booksphere.model.Routes

fun NavController.navigateToScreenAddBook(navController: NavHostController){
    navController.navigate(Routes.ScreenAddBook.route)
   }


fun NavGraphBuilder.addBookScreen(navController: NavController) {
    composable(Routes.ScreenAddBook.route) { AddBookScreen() }
}
*/